# Database Configuration
# ---------------------
# DB_HOST - Database server hostname (e.g., localhost or 127.0.0.1)
DB_HOST=localhost

# DB_PORT - PostgreSQL server port (default: 5432)
DB_PORT=5432

# DB_USER - PostgreSQL username for FlipDeck application
# This should be the user created in the setup script (flipdeck_user)
DB_USER=flipdeck_user

# DB_PASSWORD - Password for the database user
# This should match the password set in the setup script
DB_PASSWORD=#swordfish#

# DB_DATABASE - Name of the database for the application
DB_DATABASE=flashcards

# Application Configuration
# -----------------------
# PORT - The port number the application will listen on
PORT=3000

# NODE_ENV - The environment mode (development, production, test)
NODE_ENV=development